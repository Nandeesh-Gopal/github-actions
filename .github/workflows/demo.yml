name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
      - with:
          node-version:18
      - run: npm install
      - run: npm test